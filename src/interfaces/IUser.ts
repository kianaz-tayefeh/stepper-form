export interface IUser {
  name: string;
  phone: string;
  email: string;
  gender: string;
  dateOfBirth: string;
  address: string;
  spokenLanguage: string | null;
  weight: number | null;
  height: number | null;
  isConfirm: string;
}
